#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        int cnt1 = 0;
        int cnt2 = 0;
        int cnt3 = 0;

        int n;
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            string s1;
            cin >> s1;
        }
        for (int i = 0; i < n; i++)
        {
            string s2;
            cin >> s2;
        }
        for (int i = 0; i < n; i++)
        {
            string s3;
            cin >> s3;
        }
    }

    return 0;
}